<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ticket Wizard!</title>
    <link rel="icon" type="image/x-icon" href="./images/favicon.ico">
</head>
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"
    />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=Hammersmith+One&family=Noto+Color+Emoji&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <nav
      class="d-flex flex-row justify-content-between align-items-center mx-2"
    >
      <div
        class="col-6 ps-2 pt-3 pb-2 my-3 hammersmith-one-regular text-nowrap"
        id="title"
      ><a href="/"><span class="noto-color">🔮</span>
          <span>Ticket</span>Wizard<span>!</span></a></div>

      <div class="d-block d-md-none dropdown p-3">
        <button
          class="btn btn-contrast dropdown-toggle btn-sm"
          type="button"
          data-bs-toggle="dropdown"
          aria-expanded="false"
        >Account</button>

        {{#unless logged_in}}
          <ul class="dropdown-menu">
            <li><a
                class="dropdown-item"
                href="/login"
                id="menuLogin"
              >Login</a></li>
            <li><a
                class="dropdown-item"
                href="/login"
                id="menuCreateAccount"
              >Create Account</a></li>
          </ul>
        {{/unless}}

        {{#if logged_in}}
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/purchases" id="menuLogin">View
                Account</a></li>
            <li><a class="dropdown-item" href="#" id="logout">Logout</a></li>
          </ul>
        {{/if}}
      </div>

      {{#if logged_in}}
        <div class="d-none d-md-block p-3">
          <a href="/purchases"><button class="btn btn-light me-3" id="viewAcct">View Account</button></a>
          <button class="btn btn-light" id="logout">Logout</button>
        </div>
      {{/if}}
    </nav>
    <main class="mx-2 mobileMain">
      {{{body}}}
    </main>
    <footer class="mx-2">
      <div class="text-center border-top border-2">
        <p class="p-4">&copy;2024 Daniel Brandt, Joe Preimesberger, Jonathon
          Blom, Pete Maynard, Travis Parks</p>
      </div>
    </footer>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>

</html>