<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ticket Wizard!</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="../../public/style.css" />
  </head>

  <body>
    <div class="container-fluid">
      <nav
        class="d-flex flex-row justify-content-between align-items-center border-bottom border-2"
      >
        <div class="col-6 p-3" id="title">🔮 Ticket Wizard!</div>
        {{#if logged_in}}
          <div class="p-3">
            <button class="btn btn-light" id="viewAcct">View Account</button>
            <button class="btn btn-light" id="logout">Logout</button>
          </div>
        {{/if}}
      </nav>
    </div>
    <main class="container container-fluid mt-5">
      {{{body}}}
    </main>
    <footer class="container-fluid">
      <div
        class="d-flex flex-row justify-content-center align-items-center border-top border-2"
      >
        <p class="pt-4">&copy;2024 Daniel Brandt, Joe Preimesberger, Jonathon
          Blom, Pete Maynard, Travis Parks</p>
      </div>
    </footer>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>